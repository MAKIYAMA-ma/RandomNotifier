<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RandomNotifier" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="buttonStyle">@style/CustomButtonStyle</item> <!-- カスタムボタンスタイルを適用 -->
    </style>

    <style name="Theme.RandomNotifier" parent="Base.Theme.RandomNotifier" />

    <style name="CustomButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/custom_button_background</item>
        <item name="android:padding">12dp</item>
        <item name="android:textColor">#000000</item> <!-- 文字の色を変更 -->
    </style>
</resources>
